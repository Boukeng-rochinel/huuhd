odoo.define('edutek_core.dashboard_ext', function(require) {
    "use strict";
    // Etait auparavant utilise pour ajouter au tableau de bord des
    // parametres un encart pointant vers l'editeur d'origine (logo recupere
    // via le serveur de l'editeur, en lui envoyant l'UUID de la base).
    // Retire : pas d'equivalent EduTek, et evite d'envoyer l'UUID de la
    // base a un tiers sans relation contractuelle avec ce projet.
});
