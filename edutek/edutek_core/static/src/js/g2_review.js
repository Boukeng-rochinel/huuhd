/** @odoo-module **/

// G2 review nag popup pointing at the original vendor's feedback page and
// hotlinking an image from their domain - removed (not referenced by any
// asset bundle in __manifest__.py, so this file was never actually loaded).
